# Post-Launch Plan

## v3.0 — Store Launch
Ship current RC once external signing + device + IAP sandbox pass.

## v3.0.1 — Hotfixes
Crash/save/store-review only.

## v3.1 — First real user requests
Copy polish, small content, music if licensed, graphic variants.

## v3.2 — Content / Events / Transport depth
More events; bus UX depth; performance on mid Android.

## v4 — Only if demand justifies
Cloud save backend, rail/metro after bus proven, optional accounts (never required for core).
