/** Store shipping gates for IAP — architecture kept, production exposure controlled. */

/**
 * Real-money IAP for first production submit.
 * FALSE until Apple + Google sandbox acceptance is evidenced on device.
 * Soft-currency (in-game cash) shop remains fully available.
 */
export const IAP_ENABLED_FOR_PRODUCTION = false;
